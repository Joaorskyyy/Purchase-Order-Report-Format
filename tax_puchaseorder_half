<t t-name="account.tax_purchaseorder_half">
    <!--
        Generic template to display a list of tax groups with the related amounts.

        ARGUMENTS:
        - tax_totals: dict in the form generated by account.move's _get_tax_totals.
        - subtotal_to_show: The subtotal we need to render the groups from
    -->
    
    <t t-foreach="tax_totals['groups_by_subtotal'][subtotal_to_show]" t-as="amount_by_group">
        <tr>
            <t t-if="tax_totals['display_tax_base']">
                <td class="text-end pe-1">
                    <span t-out="amount_by_group['tax_group_name']"/>
                    <span class="text-nowrap">
                         <!--on-->
                    <!--<span t-esc="'{:,.2f} ₱'.format(float(amount_by_group['formatted_tax_group_base_amount'].replace(',', '').replace('\xa0', '').replace('₱', '')))"/>-->
                        <!--<t t-out="amount_by_group['formatted_tax_group_base_amount']"/>-->
                    </span>
                </td>
                <td class="text-end pe-1 o_price_total">
                    <span class="text-nowrap" t-esc="'₱ {:,.2f} '.format(float(amount_by_group['formatted_tax_group_amount'].replace(',', '').replace('\xa0', '').replace('₱', '')))"/>
                    <!--<span class="text-nowrap" t-out="amount_by_group['formatted_tax_group_amount']"/>-->
                </td>
            </t>
            <t t-else="">
                <td class="text-end pe-1"><span class="text-nowrap" t-out="amount_by_group['tax_group_name']"/></td>
                <!--<td class="text-end o_price_total">-->
                <!--    <span class="text-nowrap" t-out="amount_by_group['formatted_tax_group_amount']"/>-->
                <!--</td>-->
                <td class="text-end pe-1 o_price_total">
                    <span class="text-nowrap" t-esc="'₱ {:.2f} '.format(float(amount_by_group['formatted_tax_group_amount'].replace('\xa0', '').replace('₱', '')))"/>
                </td>
            </t>
        </tr>
    </t>
</t>